# Clay Third-Party Package

`lib/thirdparty/clay/` contains the bundled Clay single-header package used by the display UI stack
and the SDL simulator.

Treat it as external code:

- do not add project feature code here
- keep local changes isolated and intentional
- update docs/tests together when the bundled Clay version changes
